# 코드 리뷰 후속 작업 체크리스트

검토 기준일: 2026-09-01

## P1 — 퍼즐 해법 탐색 보완

- [x] `g1`의 첫 번째 후보만 고정하지 않고, 가능한 `g1`과 `g2` 조합을 함께 백트래킹한다.
- [x] 각 부분 그래프가 `R`, `G`, `P`, `Y` 모두에 대해 정확히 차수 2인지 명시적으로 검증한다.
- [x] 반례 시드 `FD81F29052D3`이 해답 없음으로 오판되지 않는 회귀 테스트를 추가한다.
- [x] 기본 퍼즐 3종에 대해 그래프 분해와 최종 큐브 방향 적용을 검증한다.

완료 조건: 해가 존재하는 퍼즐을 첫 후보 선택 순서와 무관하게 찾고, 반례 및 기본 퍼즐 테스트가 통과한다.

## P1 — 빌드 및 린트 의존성 정상화

- [x] `typescript`, `eslint`, `@eslint/js`, `globals`, `typescript-eslint`, `eslint-plugin-react-hooks`, `eslint-plugin-react-refresh`를 적절한 개발 의존성으로 선언한다.
- [x] 빌드 도구와 타입 패키지를 `dependencies`와 `devDependencies`로 올바르게 분류한다.
- [x] `package.json`, `package-lock.json`, `package-lock 2.json`의 차이를 검토해 하나의 잠금 파일만 남긴다.
- [x] 깨끗한 환경에서 `npm ci`를 실행한다.
- [x] `npm run build`와 `npm run lint`를 통과시킨다.
- [x] `npm run deploy`의 `predeploy` 단계가 정상 동작하는지 확인한다.

완료 조건: 저장소를 새로 복제한 환경에서 설치, 린트, 빌드가 추가 조치 없이 재현된다.

## P1 — 사용자 정의 퍼즐과 시드 무결성

- [x] 개별 면 입력을 `R`, `G`, `P`, `Y`로 제한한다.
- [x] 여섯 글자 붙여넣기에도 동일한 색상 검증을 적용한다.
- [x] 비어 있거나 잘못된 면이 있으면 게임 시작을 차단하고 해당 위치를 안내한다.
- [x] `X`는 공식 시드에 포함하지 않고 미완성 상태를 설명하는 개념으로만 제한한다.
- [x] 시드 생성 후 다시 파싱했을 때 원본 퍼즐과 동일한지 왕복 테스트를 추가한다.
- [x] 클립보드 권한 거부 시 사용자에게 실패 메시지를 표시한다.

완료 조건: 시작 가능한 모든 사용자 퍼즐이 시드 생성·복원 과정에서 손실 없이 유지된다.

## P2 — 모바일 하단 컨트롤

- [x] 강의자 모드의 버튼 5개가 320px 너비 화면에서도 잘리지 않도록 크기와 간격을 반응형으로 조정한다.
- [x] 작은 화면에서는 버튼 크기와 간격을 줄이는 축소 레이아웃을 적용한다.
- [x] 하단 safe-area inset을 고려한다.
- [x] 320px, 360px, 390px 너비에서 일반 모드와 강의자 모드를 확인한다.

완료 조건: 지원 대상 모바일 화면에서 모든 버튼을 가림 없이 누를 수 있다.

## P2 — 접근성

- [x] 문서 언어를 한국어(`lang="ko"`)로 지정한다.
- [x] 아이콘 전용 버튼에 기능을 설명하는 `aria-label`을 추가한다.
- [x] 입력 요소에 프로그램적으로 연결된 레이블을 제공한다.
- [x] 키보드만으로 홈, 편집기, 게임 컨트롤을 사용할 수 있는지 확인한다.
- [x] 포커스 표시와 색상 대비를 점검한다.

완료 조건: 주요 흐름을 키보드와 화면 판독기로 이해하고 조작할 수 있다.

## 테스트 기반 마련

- [x] 시드 생성·파싱 단위 테스트를 추가한다.
- [x] 그래프 해법 탐색 단위 테스트를 추가한다.
- [x] 회전 행렬의 24개 방향과 면 매핑을 검증한다.
- [x] 홈 → 사용자 퍼즐 → 게임 시작 흐름의 UI 스모크 테스트를 추가한다.
- [x] CI에서 린트, 타입 검사, 테스트, 프로덕션 빌드를 실행한다.
